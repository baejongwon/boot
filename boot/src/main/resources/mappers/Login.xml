<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="com.care.boot.login.ILoginMapper">
    
	<select id="getMember">
		SELECT * FROM db_quiz WHERE id =#{id}
	</select>
	
	<select id="checkLogin">
		SELECT pw FROM db_quiz WHERE id=#{id}
	</select>
    </mapper>